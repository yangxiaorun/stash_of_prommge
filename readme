1.从滴滴给的原始数据中提取数据量比较多的司机，放在文件夹按照目录存储好（处理好的数据可以直接高德地图可视化）
2.计算每个个司机的各种统计量属性，并将其存入一个csv文件（包括每个轨迹点的多维属性，以及整个轨迹点统计变量）
  利用生成的统计变量手动为每个司机打上标签
  将标签存成字典格式放在文件中，供以后读取
3.利用2提供的字典标签，将数据制作成为卷积神经网络需要的数据集与标签。其中每个数据集与标签以日为单位
4.将第3步的数据集与标签混合成完整的数据集与标签（由于numpy数据格式的原因混合比较耗时）